
[user]
  name = lima
  email = lima@mbp
[core]
  editor = vim
  mergeoptions = --no-edit
[http]
  sslVerify = false
[url "https://github.com/"]
  insteadOf = git://github.com/
[credential]
  helper = store
[init]
  defaultBranch = main
	templatedir = ~/.git_templates
[pull]
# `git rebase –abort` when we need to reset the changes
	rebase = true
[merge]
	tool = nvimdiff4
	prompt = false
[mergetool "nvimdiff4"]
  # requires tpope/fugitive
  cmd = vim -f -c \"Gdiffsplit!\" \"$MERGED\"
[mergetool]
	keepBackup = false
	prompt = false
[push]
	autoSetupRemote = true
	default = simple

[diff]
	colorMoved = zebra
