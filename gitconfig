
[user]
name = lima
email = lima@mbp
  
[core]
editor = vim
mergeoptions = --no-edit
  
[http]
sslVerify = false
  
[url "https://github.com/"]
insteadOf = git://github.com/
  
[credential]
helper = store
  
[init]
defaultBranch = main
templatedir = ~/.git_templates
  
[pull]
# requires git > 2.6
# `git rebase –abort` when we need to reset the changes
rebase = true
  
[merge]
 tool = vimdiff
 conflictstyle = diff3

[mergetool]
 keepBackup = false
 prompt = false

[mergetool "vimdiff"]
 cmd = "vim  -d $MERGED $LOCAL $BASE $REMOTE -c 'wincmd J | wincmd ='"
  
[push]
 autoSetupRemote = true
	
# push only the default branch
default = simple

[diff]
colorMoved = zebra
