// function expression
// variable has a function as its value
// function is anonymous because it has no name
// but variable points to it so it will run
let greetMe = function () {
  console.log('hi tony');
}

debugger;

greetMe();

// its still first class
