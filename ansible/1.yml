---
# Author: Jose Lima
# Date: 2023-07-31 18:27 CST

- name: "Testing go mod"
  hosts: localhost
  # order: shuffle
  gather_facts: false
  # serial: 10

  tasks:
    - name: "Testing --> "
      go_command:
        command: /system/clock/print
        provider:
          username: admin
          password: jocker
          host: 100.64.0.1
      # debugger: always
      register: out

    - name: Debug
      ansible.builtin.debug:
        msg: |
          {{ out.stdout }}
